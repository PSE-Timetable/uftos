package de.uftos.repositories.solver;

public interface SolverAdapter {
    //todo: solve-method (replace "Object")
    /**
     * Solves the problem instance "timetable" under the given constraints.
     * @param timetable the problem instance
     * @param constraintInstances the instances of the relevant constraints
     * @param constraintDefinitions the definitions of the relevant constraints
     * @return a solved timetable instance
     */
    //Object solve(Object timetable, Object constraintInstances, Object constraintDefinitions);
}
